<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.smaboy.demo.mapper.UserMapper">
    <resultMap id="BaseResultMap" type="com.smaboy.demo.entity.User">
        <result column="username" jdbcType="VARCHAR" property="username" />
        <result column="password" jdbcType="VARCHAR" property="password" />
        <result column="phone" jdbcType="VARCHAR" property="phone" />
    </resultMap>
    <insert id="addUserInfo" parameterType="com.smaboy.demo.entity.User">
        INSERT INTO `user` (username, password, phone) VALUES(#{username},#{password},#{phone})
    </insert>

    <select id="queryByUsername" resultType="com.smaboy.demo.entity.User">
        SELECT * FROM `user` WHERE username = #{username}
    </select >

</mapper>